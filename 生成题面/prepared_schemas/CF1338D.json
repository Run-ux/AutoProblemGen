{
  "problem_id": "CF1338D",
  "source": "codeforces",
  "input_structure": {
    "type": "tree",
    "length": {
      "min": 3,
      "max": 100000
    },
    "value_range": {
      "min": 1,
      "max": 100000
    },
    "properties": {
      "connected": true,
      "acyclic": true,
      "simple": true
    },
    "components": []
  },
  "core_constraints": {
    "constraints": [
      {
        "name": "overlap_constraint",
        "description": "任意两个橡皮筋在平面上相交的充要条件是对应顶点在输入树中存在边相连。",
        "formal": "intersect(u, v) \\iff (u, v) \\in E",
        "source_sections": [
          "description"
        ]
      },
      {
        "name": "order_constraint",
        "description": "嵌套序列要求序列中相邻元素满足严格的包含偏序关系,即对于序列中所有 i \\ge 2,前一个橡皮筋必须完全包含后一个橡皮筋,且包含关系隐含两者不相交。",
        "formal": "\\forall i \\in [2, k], includes(a_{i-1}, a_i)",
        "source_sections": [
          "description"
        ]
      }
    ]
  },
  "objective": {
    "type": "maximize_value",
    "description": "最大化嵌套序列的长度",
    "target": "length",
    "requires_solution": false
  },
  "invariant": {
    "invariants": [
      {
        "name": "state_transition",
        "description": "算法为每个节点维护两个状态值,分别对应该节点在嵌套序列中作为端点或中间节点时的子树最大长度。状态转移通过子节点状态的最大值聚合与当前节点度数偏移修正完成,并在合并不同子树状态时更新全局最优解。状态定义与转移规则在自底向上计算过程中保持严格一致。",
        "properties": {},
        "evidence_source": "solution_code"
      },
      {
        "name": "dependency_order",
        "description": "状态计算严格遵循树的后序遍历依赖关系。算法通过显式构建遍历栈并逆序迭代,保证任意节点的所有子节点状态均在父节点状态计算前完全收敛,消除循环依赖并满足树形DP的推进顺序。",
        "properties": {},
        "evidence_source": "solution_code"
      }
    ]
  }
}