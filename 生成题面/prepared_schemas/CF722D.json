{
  "problem_id": "CF722D",
  "source": "codeforces",
  "input_structure": {
    "type": "array",
    "length": {
      "min": 1,
      "max": 50000
    },
    "value_range": {
      "min": 1,
      "max": 1000000000
    },
    "properties": {
      "distinct": true
    },
    "components": []
  },
  "core_constraints": {
    "constraints": [
      {
        "name": "operation_type",
        "description": "集合X变换为集合Y仅允许执行两种操作:将任意元素乘以2,或将任意元素乘以2后加1。Y中的每个元素必须由X中的某个元素经过若干次上述操作生成。",
        "formal": "∀y∈Y, ∃x∈X, 使得y可通过有限次 v→2v 或 v→2v+1 操作从x推导得出",
        "source_sections": [
          "description"
        ]
      },
      {
        "name": "distinctness",
        "description": "集合X与集合Y均由n个互不相同的正整数构成。",
        "formal": "|X| = |Y| = n, 且X与Y内部元素两两互异",
        "source_sections": [
          "description",
          "input"
        ]
      }
    ]
  },
  "objective": {
    "type": "minimize_value",
    "description": "寻找能生成给定集合的初始集合,使其中的最大元素最小化",
    "target": "max_element",
    "requires_solution": true
  },
  "invariant": {
    "invariants": [
      {
        "name": "monotonicity",
        "description": "算法在每次迭代中固定选取当前集合的最大元素,并沿二叉树父节点方向（整除2）连续下探,直至找到首个未出现在集合中的祖先位置进行替换。该策略保证集合的最大元素值在整个执行过程中单调不增,且每次有效替换均使优化目标严格下降或推进至不可再降的边界,从而确保最终结果满足最小化最大值的要求。",
        "properties": {
          "max_element_trend": "non_increasing",
          "selection_policy": "always_current_maximum",
          "replacement_rule": "deepest_unoccupied_ancestor",
          "termination_condition": "ancestor_hits_zero_or_all_ancestors_occupied"
        },
        "evidence_source": "solution_code"
      }
    ]
  }
}