{
  "problem_id": "CF1513D",
  "source": "codeforces",
  "input_structure": {
    "type": "composite",
    "length": {
      "min": null,
      "max": null
    },
    "value_range": {
      "min": null,
      "max": null
    },
    "properties": {
      "multiple_test_cases": true
    },
    "components": [
      {
        "role": "parameters",
        "role_description": "number of vertices and constant edge weight",
        "type": "tuple",
        "length": {
          "min": 2,
          "max": 2
        },
        "value_range": {
          "min": 1,
          "max": 1000000000
        },
        "properties": {}
      },
      {
        "role": "vertex_values",
        "role_description": "sequence of positive integers assigned to vertices",
        "type": "array",
        "length": {
          "min": 2,
          "max": 200000
        },
        "value_range": {
          "min": 1,
          "max": 1000000000
        },
        "properties": {
          "ordered": true
        }
      }
    ]
  },
  "core_constraints": {
    "constraints": [
      {
        "name": "divisibility",
        "description": "对于任意顶点对 i 和 j (i < j),当且仅当子数组 a[i..j] 的最小值能整除该子数组中的所有元素时,i 和 j 之间存在一条边,边权等于该最小值。",
        "formal": "gcd(a_i, ..., a_j) = min(a_i, ..., a_j) ⇔ edge(i, j) with weight min(a_i, ..., a_j)",
        "source_sections": [
          "description"
        ]
      },
      {
        "name": "adjacency_relation",
        "description": "任意相邻索引对应的顶点 i 与 i+1 之间始终存在一条边,其权重固定为给定的整数 p。",
        "formal": "∀ i ∈ [1, n-1], edge(i, i+1) exists with weight p",
        "source_sections": [
          "description"
        ]
      }
    ]
  },
  "objective": {
    "type": "minimize_value",
    "description": "计算给定图的最小生成树总权重的最小值",
    "target": "total_weight",
    "requires_solution": false
  },
  "invariant": {
    "invariants": [
      {
        "name": "additivity",
        "description": "The minimum spanning tree weight decomposes into the sum of minimum costs required to cross each of the n-1 adjacent cuts. Because all valid extra edges span contiguous intervals, the optimal connection cost for each adjacent gap can be determined independently, allowing the global optimum to be computed via direct summation.",
        "properties": {},
        "evidence_source": "both"
      },
      {
        "name": "monotonicity",
        "description": "The algorithm maintains a running candidate minimum that extends unidirectionally across the array as long as the divisibility condition holds. Upon encountering a non-multiple, the candidate resets to the current element, ensuring each position is processed in a single forward or backward sweep without backtracking or re-evaluation.",
        "properties": {},
        "evidence_source": "solution_code"
      }
    ]
  }
}