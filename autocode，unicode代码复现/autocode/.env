# OpenAI API 配置
# 请填写你的API密钥和基础URL

# API密钥（必需）
OPENAI_API_KEY=sk-97576a951556441286bce0c6922abff6

# API基础URL（可选，默认为 https://api.openai.com/v1）
# 如果使用国内API服务，请修改为相应的URL
OPENAI_BASE_URL=https://api.deepseek.com

# 模型名称（可选，默认为 gpt-4o）
OPENAI_MODEL=deepseek-v4-pro

# python -m autocode.generator \
#   --input autocode/input/test_batch \
#   --batch \
#   --transformations 2 \
#   --use-llm

# --batch 启用批量模式 --input 输入目录路径（批量模式） --output 输出目录路径（可选，不指定则自动生成）